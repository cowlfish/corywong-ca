---
import RealestateLayout from '../../layouts/RealestateLayout.astro';
import NewsletterSignup from '../../components/NewsletterSignup.astro';
---

<RealestateLayout
  title="Contact Cory Wong | Real Estate Agent | Trustwell Realty Inc., Brokerage"
  description="Book a free consultation with Cory Wong, Real Estate Agent, Trustwell Realty Inc., Brokerage. Serving Toronto, Markham, Richmond Hill, and the broader GTA."
  canonicalPath="/contact"
>
  <section class="page-hero">
    <div class="container">
      <h1>Let's Connect</h1>
      <p class="page-hero__subtitle">
        Whether you're buying, selling, or just getting started, reach out.
        There's no commitment attached to a first conversation.
      </p>
    </div>
  </section>

  <section class="calendly-section">
    <div class="container">
      <h2>Book a Consultation</h2>
      <p class="calendly-section__subtitle">A 30-minute consultation is free. Pick a time below.</p>
      <div class="calendly-embed" id="calendly-container">
        <div class="calendly-inline-widget" data-url="https://calendly.com/trustwell-corywong/general" style="min-width:320px;height:700px;"></div>
      </div>

      <noscript>
        <div class="noscript-fallback">
          <h3>JavaScript Required for Online Booking</h3>
          <p>Please enable JavaScript to use the online booking calendar, or use the contact form below to get in touch.</p>
        </div>
      </noscript>
    </div>
  </section>

  <section class="contact-fallback" id="contact-form">
    <div class="container">
      <h2>Prefer to Reach Out by Form?</h2>
      <form class="lead-form" id="contact-form-el" data-form-type="contact">
        <div class="form-row">
          <div class="form-field">
            <label for="contact-name">Name</label>
            <input type="text" id="contact-name" name="name" required autocomplete="name" />
          </div>
          <div class="form-field">
            <label for="contact-email">Email</label>
            <input type="email" id="contact-email" name="email" required autocomplete="email" />
          </div>
        </div>
        <div class="form-field">
          <label for="contact-phone">Phone (optional)</label>
          <input type="tel" id="contact-phone" name="phone" autocomplete="tel" />
        </div>
        <div class="form-field">
          <label for="contact-message">Message</label>
          <textarea id="contact-message" name="message" rows="4" required></textarea>
        </div>
        <button type="submit" class="form-submit">Send Message</button>
        <p class="form-status" id="contact-form-status" aria-live="polite"></p>
      </form>
    </div>
  </section>

  <NewsletterSignup heading="Stay Informed" />
</RealestateLayout>

<script>
  const script = document.createElement('script');
  script.src = 'https://assets.calendly.com/assets/external/widget.js';
  script.async = true;
  document.head.appendChild(script);

  const form = document.getElementById('contact-form-el') as HTMLFormElement;
  const status = document.getElementById('contact-form-status') as HTMLElement;

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    status.textContent = 'Sending...';
    status.className = 'form-status';

    const data = Object.fromEntries(new FormData(form));

    try {
      const res = await fetch('/api/lead', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ ...data, type: 'contact' }),
      });

      if (res.ok) {
        status.textContent = 'Thank you! I\'ll get back to you soon.';
        status.classList.add('form-status--success');
        form.reset();
      } else {
        throw new Error('Failed');
      }
    } catch {
      status.textContent = 'Something went wrong. Please try again or book directly via the calendar above.';
      status.classList.add('form-status--error');
    }
  });
</script>

<style>
  .page-hero {
    padding: var(--space-2xl) 0 var(--space-xl);
    text-align: center;
  }

  .page-hero h1 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: var(--space-md);
  }

  .page-hero__subtitle {
    font-size: 1.125rem;
    color: var(--color-text-muted);
    max-width: 34rem;
    margin: 0 auto;
    line-height: 1.6;
  }

  .calendly-section {
    padding: 0 0 var(--space-xl);
  }

  .calendly-section h2 {
    text-align: center;
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: var(--space-xs);
  }

  .calendly-section__subtitle {
    text-align: center;
    color: var(--color-text-muted);
    margin-bottom: var(--space-lg);
  }

  .calendly-embed {
    max-width: 40rem;
    margin: 0 auto;
    border: 1px solid var(--color-border);
    border-radius: 12px;
    overflow: hidden;
  }

  .noscript-fallback {
    text-align: center;
    padding: var(--space-xl);
    background: #f5f5f4;
    border-radius: 12px;
  }

  .noscript-fallback h3 {
    font-size: 1.25rem;
    margin-bottom: var(--space-sm);
  }

  .noscript-fallback p {
    color: var(--color-text-muted);
  }

  .contact-fallback {
    padding: var(--space-xl) 0;
    border-top: 1px solid var(--color-border);
  }

  .contact-fallback h2 {
    font-size: 1.5rem;
    font-weight: 600;
    text-align: center;
    margin-bottom: var(--space-lg);
  }

  .lead-form {
    max-width: 32rem;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-md);
  }

  .form-field {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .form-field label {
    font-size: 0.875rem;
    font-weight: 500;
  }

  .form-field input,
  .form-field textarea {
    padding: 0.625rem 0.75rem;
    border: 1px solid var(--color-border);
    border-radius: 6px;
    font-size: 0.9375rem;
    font-family: var(--font-sans);
    background: var(--color-surface);
    transition: border-color 0.15s ease;
  }

  .form-field input:focus,
  .form-field textarea:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(15, 118, 110, 0.1);
  }

  .form-submit {
    padding: 0.75rem 1.75rem;
    border-radius: 6px;
    font-weight: 600;
    font-size: 0.9375rem;
    background: var(--color-accent);
    color: #ffffff;
    border: none;
    cursor: pointer;
    transition: background-color 0.15s ease;
  }

  .form-submit:hover {
    background: var(--color-accent-hover);
  }

  .form-status {
    text-align: center;
    font-size: 0.875rem;
    min-height: 1.25rem;
  }

  .form-status--success {
    color: var(--color-accent);
  }

  .form-status--error {
    color: #dc2626;
  }

  @media (max-width: 640px) {
    .form-row {
      grid-template-columns: 1fr;
    }
  }
</style>
